name: Setup Railway
description: Install the Railway CLI for trigger re-re-deploy

runs:
  using: composite
  steps:
    - name: Install Railway CLI
      shell: bash
      run: npm install -g @railway/cli
